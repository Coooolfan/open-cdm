// import { instance } from '../instance';

// export const validExprFilter = (param) => instance.post('datarule/validexprfilter', param);

// export const testTransformScipt = (param) => instance.post('datarule/testtransformscript', param);

// export const validTargetWhereCondition = (param) => instance.post('datarule/validtargetwherecondition', param);
