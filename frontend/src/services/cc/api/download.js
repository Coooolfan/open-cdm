// import { instance } from '../download';

// export const downloadPackage = (param) => instance.post('jarpackage/download', param);

// export const downloadTaskLog = (param) => instance.post('logview/export/tasklog', param);

// export const downloadWorkerLog = (param) => instance.post('logview/export/workerlog', param);
