// import { instance } from '../instance';

// export const listcatalog = (param) => instance.get('auth/listcatalog', { params: param });

// export const listPureUrl = (param) => instance.get('auth/listpureurl', { params: param });
